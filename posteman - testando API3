//na pasta tasksController.js verifica se a porta está recebendo a mensagem, testando pelo posteman usando - http://localhost:3333/tasks

const getAll = (request, response) => {
    return response.status(200).json({message: 'controller está tudo certo!'});
};

module.exports = {
    getAll
};

//sendo assim,testando com o get recebendo a devida mensagem
